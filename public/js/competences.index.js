/**
 * Script JS uniquement pour la page des competences
 */
/*$(document).ready(function(){
	// On cache au chargement de la page
	$(".list-group-item-text").hide();
	// Detection du click pour afficher
	$(".list-group-item").on('click', function(e) {
		e.preventDefault();
		$(".list-group-item-text").slideUp();
		if (e.target.nextSibling.id != "") {
			var id = e.target.nextSibling.id;
		} else {
			var id = e.target.childNodes[1].id
		}
		$("#"+id).slideDown();
	});
});*/
